# 03 Prediction

예측 데이터셋과 모델 준비 파트를 모으는 기준 폴더다.

현재 실사용 작업물은 `workspace/` 안에 있다.
기존 경로 `works/prediction`은 이 위치를 가리키는 링크로 유지한다.

세부 구분:

- `01_dataset_design`
- `02_data`
- `03_images`
- `04_scripts`

발표 이후에는 `workspace/` 안의 파일을 설계, 데이터, 이미지, 스크립트 기준으로 추가 분류한다.
