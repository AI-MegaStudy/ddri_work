# 02 Data Collection

외부 데이터 수집 파트를 모으는 기준 폴더다.

현재 실사용 작업물은 각 `workspace/` 안에 있다.
기존 경로 `works/calendar`, `works/weather`는 이 위치를 가리키는 링크로 유지한다.

세부 구분:

- `01_calendar`: 공휴일 API 수집
- `02_weather`: 날씨 수집

발표 이후에는 `workspace/` 안의 파일을 수집 목적과 결과물 유형 기준으로 추가 분류한다.
