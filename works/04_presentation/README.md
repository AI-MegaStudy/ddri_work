# 04 Presentation

발표용 산출물을 모으는 기준 폴더다.

현재 실사용 작업물은 `workspace/` 안에 있다.
기존 경로 `works/presentation`은 이 위치를 가리키는 링크로 유지한다.

세부 구분:

- `01_clustering`: 군집화 발표 자료
- `02_project`: 전체 프로젝트 발표 자료

발표 이후에는 `workspace/` 안의 파일을 군집화 발표와 전체 프로젝트 발표 기준으로 추가 분류한다.
